#include<stdio.h>
int main()
{
	long array[50],*max,size,c,location=1;
	printf("Enter the number of elements\n");
	scanf("%ld",&size);
	printf("Enter the %ld integers \n",size);
	for(c=0;c<size;c++)
		scanf("%ld",&array[c]);
	max
}
